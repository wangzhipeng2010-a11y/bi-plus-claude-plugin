# BI Plus Claude Code Plugin — AI 协作契约

@AGENTS.md

> 若上面的 `@AGENTS.md` 引用未自动展开，请直接阅读同目录 `AGENTS.md`：
> 那是本项目 AI 协作的**唯一契约**，含五大准则、Plugin 边界红线、文档索引、本地 AI review 用法。
> 契约只写在 `AGENTS.md` 一处，本文件不重复正文，避免两边不同步。
